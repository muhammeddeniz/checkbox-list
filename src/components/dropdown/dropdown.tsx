import React from "react";

import "./dropdown.scss";

interface IProps {
  title?: string;
  placeholder?: string;
  section?: Array<string>;
}

const Dropdown: React.FC<IProps> = ({ title, placeholder, section }) => {
  return (
    <div>
      <h1></h1>
    </div>
  );
};

export default Dropdown;
